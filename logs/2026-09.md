# 2026-09 Change Log

## 2026-09-03 17:22:00 +0800 — 新增华益彭氏祭祖旅行手册

- 类型：独立静态页 / 客户旅行手册 / 部署
- 改动：把客户提供的「彭氏祭祖 · 潮汕九日行」完整静态站接入 `public/hwayik/`，保留原有 HTML、CSS、旅行内容、华益联络资料与图片素材；补上正式 canonical、Open Graph URL 和 Twitter 分享图片地址。
- 改动：README 记录 `/hwayik/` 的线上与本地访问方式；Vite 开发服务器为 `/hwayik` 补上尾斜线跳转并直接提供独立页面，built-site smoke 将页面、样式和分享图纳入必需产物。
- 原因：让客户预览站随 Eden 现有 GitHub Pages 流程发布到 `https://eden-tan.com/hwayik/`，无需另建应用或部署链。
- 影响：主站 React 路由、导航与双语内容不变；新旅行手册作为独立静态页面发布。
- 验证：运行 `npm run log:index` 与完整 `npm run check`，并检查 production build 中 `/hwayik/` 页面和相对资源。
- 后续：无。

## 2026-09-09 — 移除华益彭氏祭祖旅行手册

- 类型：独立静态页 / 内容下线 / 部署
- 改动：删除 `public/hwayik/` 的页面、样式、分享图与全部旅行图片，并移除对应 Vite 开发入口、built-site smoke 必需产物和 README 路由记录。
- 原因：用户要求下线 `https://eden-tan.com/hwayik/`。
- 影响：下一次 GitHub Pages 部署后，`/hwayik/` 不再发布客户旅行手册；Eden 主站与其他独立静态页不受影响。
- 验证：运行 `npm run log:index` 与完整 `npm run check`，确认 production build 不再包含 `dist/hwayik/`。
- 后续：无。

## 2026-09-09 — 文章流量与订阅增长计划

- 改动：新增根目录 `plan.md`，整理关键词候选池、验证流程、首轮文章、订阅承接、执行节奏、评估指标和研究来源。
- 原因：用户需要把前一轮 digital marketing 与 personal branding 研究沉淀为可执行计划，目标为文章流量与订阅。
- 影响：仅增加规划文档；关键词搜索量与难度保持待验证，未实施文章、订阅服务或页面改动。
- 验证：运行 `npm run log:index`、`npm run check` 和发布流程 dry-run；实际结果见本次任务输出。
- 后续：依计划取得查询数据并验证首轮选题；本次不执行增长计划。

## 2026-09-09 — 执行 plan.md 首轮第 1 篇：个人品牌定位文章

- 改动：新增 `wiki/essays/personal-brand-positioning.md`（order 0，双语 JSON payload，9 节，内链 `turn-chaos-into-systems` 与 `win-before-you-fight`）；在 `seo-routes.ts` 登记 `/notes/personal-brand-positioning` 并把 `SITE_CONTENT_LASTMOD` 由 `2026-08-19` 更新为 `2026-09-09`；同步 `wiki/index.md`；重跑 `npm run wiki:build` 更新 `generated/content.ts`。
- 原因：执行 `plan.md` 第 5 节首轮文章顺序 1（主关键词 personal brand positioning / 个人品牌定位）。用户本轮确认只交付第 1 篇，先验收语气与结构再批量。
- 影响：`/notes` 归档新增一篇置顶文章，英文与 `/zh/` 静态 HTML 与 sitemap 同步产出。正文自带完整一页定位表，不依赖订阅即可获得核心答案。经验、取舍与排除项均取自本仓库与站点可核实的事实（作品集改为知识归档的节奏、真实项目清单、AGENTS.md 已声明的表达边界），未编造数据或成效。
- 验证：`npm run check` 全绿；本地 `npm run dev` 后访问 `http://localhost:4180/notes` 应看到该文置于列表首位，`http://localhost:4180/notes/personal-brand-positioning` 与 `/zh/notes/personal-brand-positioning` 显示双语正文与两条内链。
- 后续：plan.md 第 4 节关键词验证仍缺 Search Console 与 Keyword Planner 数据，无法在仓库内完成；第 6 节订阅承接因尚无任何邮件服务与表单（全仓 grep 无命中）本轮明确不做，待用户选定服务后再实施。文章 2–4 待本篇验收后继续。

## 2026-09-09 — 执行 plan.md 首轮第 2 篇：从真实项目建立内容选题库

- 改动：新增 `wiki/essays/content-pillars-from-real-projects.md`（8 节，双语，3 条 references 指向扑克房项目真实产出的三条 wiki 笔记，内链 `personal-brand-positioning` 与 `turn-chaos-into-systems`）；在 `seo-routes.ts` 登记 `/notes/content-pillars-from-real-projects`；把 essay `order` 重排为 0–10，让两篇增长系列文章按阅读顺序排在 `/notes` 前两位；同步 `wiki/index.md` 与 `generated/content.ts`。
- 原因：执行 `plan.md` 第 5 节首轮文章顺序 2（主关键词 content pillars for personal branding / 个人品牌内容规划、内容选题库），承接第 1 篇「定位之后不知道写什么」的缺口。
- 影响：`/notes` 前两位形成连贯系列。文章核心论点是「内容支柱从做完的工作里数出来，不是事先想出来」，真实例子用扑克房项目产出的三条 wiki 笔记（background-music / button-feedback / firebase-lifetime-storage），读者可点开核实；未编造项目、数据或成效。essay order 重排只影响 `/notes` 列表顺序，不改动任何 slug、路由或正文。
- 验证：`npm run check` 全绿；本地 `npm run dev` 后 `http://localhost:4180/notes` 应看到定位篇第一、选题库篇第二，`http://localhost:4180/notes/content-pillars-from-real-projects` 与 `/zh/...` 显示双语正文、三条参考与两条内链。
- 后续：文章 3（AI 内容工作流）与 4（把笔记变成文章）待续。第 4 节关键词验证仍缺 Search Console 与 Keyword Planner 数据；第 6 节订阅承接仍未实施，待用户选定邮件服务。

## 2026-09-10

### [E-2026-09-10-01] Logging switched to the fleet-wide logbook rag-v1 format

- **type**: `docs`
- **scope**: `config`
- **impact**: `cross-cutting`
- **changed**:
  - `AGENTS.md` section 4 — rewritten to the rag-v1 rules shared by the other projects:
    `## YYYY-MM-DD` day heading + `### [E-…]` entries, seven fields, `ripples` three questions,
    and the three hard rules (append via script / oldest→newest / day headings written by the script).
  - `scripts/append-log.mjs` (new) — the portable O_APPEND appender used across all projects.
  - `scripts/harness/log-append.mjs` (new) — Eden-specific wrapper that points the generic
    appender at `logs/<current MYT month>.md` instead of the root `log.md`, creating the month
    file if it does not exist yet.
  - `scripts/harness/check-log.mjs` — the evidence gate now accepts **either** rag-v1
    (`changed` / `ripples` / `verified` / `impact` / `keywords`) **or** the pre-2026-09
    five-field Chinese shape (改动 / 原因 / 影响 / 验证 / 后续).
  - `package.json` — added `log:append` and `log:check`.
  - `log.md` — frontmatter now declares `log_format: rag-v1`.
- **ripples**:
  - **The month file is chosen with `Asia/Kuala_Lumpur`, matching `check-log.mjs`.** Anyone who
    changes the timezone in one of the two files must change it in the other: on the first or
    last day of a month the appender would write into one month's file while the gate inspects
    another, and `verify:log` would fail with "Project files changed without a matching log".
  - **`npm run log:append` must NOT be pointed at the root `log.md`.** That file is a stable
    pointer to the monthly archive; writing entries there bypasses `logs/` and makes
    `verify:log` fail. The wrapper exists precisely to prevent that.
  - **Existing history in `logs/2026-04..09.md` is NOT backfilled.** Both entry shapes now
    coexist, so anything that parses these files must not assume every entry has `ripples`.
  - **`logs/index.md` still has to be regenerated** with `npm run log:index` after appending —
    the gate checks it separately, and the appender does not do it.
- **verified**:
  - This entry itself was written with `npm run log:append`, which created the `## 2026-09-10`
    day heading in `logs/2026-09.md` automatically.
  - `npm run log:check` passes: no duplicate IDs, day headings ascend oldest → newest.
  - `npm run verify:log` green after `npm run log:index`.
- **keywords**: logbook, rag-v1, log:append, O_APPEND, monthly log, logs/index.md, check-log,
  Asia/Kuala_Lumpur, day heading, ripples, change-log gate

## 2026-09-14

### [E-2026-09-14-01] SEO：产品页独立静态正文、逐 route 日期、分路由分享图、?p= shim 收口

- **type**: `feat`
- **scope**: `seo`
- **impact**: `cross-cutting`
- **changed**:
  - `seo-static-content.ts`（新）— 首页、`/project`、`/jiju-pet`、`/jiju-revamp`、`/etreporthub`、`/etreporthub-sales`、`/dr-racing`、`/poker`、`/film-gallery`、`/life-os`、`/brand-guide`、`/penneys-game`、`/cellular-automata-lab` 与三个 `/archive/*` 各一段独立双语静态正文（3 个 H2，英文约 900–1150 字符），内容取自 `App.tsx` 页面实际渲染的模块、数字与章节；`seo-prerender.ts` 优先读取它，旧的「What this page contains / Continue exploring」通用模板只剩 noindex 工具页在用。
  - `seo-routes.ts` — `RouteSeo` 新增 `datePublished` / `dateModified` / `og`；新增 `OG_IMAGES` 分享图家族表；`SITE_CONTENT_LASTMOD` 改为 `2026-09-14` 并降级为「首页日期 + 兜底」。
  - `wiki/pages/*.md`、`wiki/essays/*.md` — frontmatter 新增 `published:` / `updated:`（发布日取该 slug 首次进仓库的 commit 日，更新日取 2026-08-14 迁移进 Markdown 的日期；两篇 9 月新文为 2026-09-09）；`scripts/wiki/build.mjs` 把它们编译成 `datePublished` / `dateModified`，`scripts/wiki/lint.mjs` 强制 ISO 格式与 updated ≥ published；`generated/content.ts` 重建。
  - `seo-prerender.ts` — 新增 `routeDates` / `routeLastmod` / `routeOgType` / `routeOgImage`；JSON-LD 的 BlogPosting / TechArticle / WebPage / ProfilePage 改带 `datePublished` + `dateModified`，`image` 按 route 家族。
  - `vite.config.ts` — sitemap `lastmod` 改为 `routeLastmod(route)`；每个 route HTML 写入 `og:type`（Notes/Wiki 为 `article`）、按家族的 `og:image` / `og:image:alt` / `twitter:image`，article 追加 `article:published_time` / `article:modified_time` / `article:author`；`robots.txt` 新增 `Disallow: /*?p=` 与 `Disallow: /*&p=`。
  - `public/og/*.jpg`（新，9 张 1200×630）— 用 `sips` 从 `public/home-banners/*` 与 `public/images/eden-environmental-portrait.jpg` 裁出：jiju、etreporthub、dr-racing、poker、film-gallery、life-os、conway、notes（作者肖像，给非金融文章）、notes-finance（给五篇金融文章）。
  - `seo.ts` — 客户端同步改用同一套 `OG_IMAGES` / `routeOgType` / `routeDates`，不再把文章页的分享图与类型覆盖回全站默认；未登记路径返回 `Page not found` 标题并标 `noindex, follow`。
  - `App.tsx` — 新增 `NotFoundPage`，未登记路径不再落到首页；`styles/pages/not-found.css` + `index.css` import。
  - `scripts/harness/check-routes.mjs` — 校验日期格式与顺序、非内容 route 必须有 `datePublished`、内容 route 不得在 registry 写日期、`dateModified` 不得晚于 `SITE_CONTENT_LASTMOD`、`og` 键与 `public/og` 文件存在、可索引非 Markdown route 必须在 `seo-static-content.ts` 有正文。
  - `scripts/harness/smoke-built-site.mjs` — 逐 URL 核对 sitemap `lastmod`、要求 lastmod 不是全站同一天、核对 `og:image` 文件存在与 `og:type`、article 的 `article:published_time` 与 JSON-LD `datePublished`、可索引页静态正文最少 900（en）/ 400（zh）字符、可索引页之间 H2 集合不得重复、`robots.txt` 含 `/*?p=` Disallow。
  - `tests/unit/seo-prerender.test.ts`（新）— 6 个用例覆盖日期来源、hub 取最新子页、sitemap 日期分布、article JSON-LD、分享图家族、静态模板唯一性。
  - `README.md`、`state/current.md`、`AGENTS.md`（Route / SEO Registry Rules 新增三条）。
- **ripples**:
  - **改一个产品页的内容，必须同步改 `seo-static-content.ts` 那一段并把 `seo-routes.ts` 里该 route 的 `dateModified` 往前推**；否则爬虫拿到的静态正文与 React 页面会分叉，而 sitemap 还说它没变。`verify:routes` 只能查缺不能查漂移。
  - **Wiki/Notes 的日期只认 Markdown frontmatter**：改文章内容要改 `updated:`；在 `seo-routes.ts` 给 `/wiki/*` 或 `/notes/*` 写 `datePublished` 会被 `verify:routes` 拒绝。新建 Markdown 文件没有 `published:` / `updated:` 时 `wiki:build` 直接抛错。
  - **`SITE_CONTENT_LASTMOD` 语义变了**：它不再代表「所有页面都在这天变了」，只是首页日期与兜底；但任何 route 的 `dateModified` 比它新会导致 `verify:routes` 失败，所以推某个 route 日期到今天时也要把它推到今天。
  - **`test:smoke` 现在要求可索引页 H2 集合两两不同**：再往 `seo-static-content.ts` 加页面时不能复制别页的三个 H2 标题；`/wiki/*` 与 noindex 页保留旧的 300/220 字符门槛。
  - **`seo.ts` 现在 import `seo-prerender.ts`**，客户端 bundle 因此带上 `seo-static-content.ts` 的文案（gzip 后主 chunk 约 166 kB）；后续拆 chunk 时注意这条依赖。
  - **未登记路径改渲染 `NotFoundPage`**：以前靠「未知路径 = 首页」的任何内部链接会变成 404 视图；`/analog-tech` 别名不受影响（仍重定向到 `/film-gallery`）。
  - **`og-image.jpg` 不再是唯一分享图**：外部缓存（LinkedIn / X）需要重新抓取才会换图；home 与 `/project`、`/brand-guide`、`/penneys-game`、`/archive/*`、`/wiki`、`/wiki/rag-flow` 仍用默认图。
- **verified**:
  - `npm run check` 全绿：verify（skills / routes / log / wiki / css / firebase）→ 单元测试（含新增 6 例）→ typecheck → functions:check → production build → test:smoke。
  - `dist/sitemap.xml` 的 `lastmod` 分布为 2026-08-14 ×32、2026-08-19 ×2、2026-09-09 ×6、2026-09-14 ×32，不再是单一日期。
  - `dist/notes/what-is-wealth/index.html` 含 `og:type=article`、`og/notes-finance.jpg`、`article:published_time=2026-07-27`、JSON-LD `datePublished/dateModified`；`dist/poker/index.html` 三个 H2 为 How it works / Designed for private games / The crew and the stories。
  - `dist/robots.txt` 含 `Disallow: /*?p=`。
  - 本地：`npm run dev` 后打开 `http://localhost:4180/does-not-exist` 应看到 404 视图与 `noindex` robots meta；`http://localhost:4180/poker` 分享 meta 指向 `/og/poker.jpg`。
- **keywords**: SEO, prerender, static body, seo-static-content, lastmod, datePublished, dateModified, frontmatter published updated, sitemap, JSON-LD, Open Graph, og:type article, og image per route, public/og, robots Disallow ?p=, soft 404, NotFoundPage, verify:routes, test:smoke

### [E-2026-09-14-02] 移除两篇尚未准备上线的个人品牌文章
- **type**: `content`
- **scope**: Notes 双语正文、路由与 Wiki 索引
- **impact**: personal-brand-positioning 与 content-pillars-from-real-projects 不再出现在文章列表、sitemap 或静态文章页面。
- **changed**: 删除两篇 wiki/essays Markdown，移除 seo-routes.ts 登记与 wiki/index.md 入口，重新编译 generated/content.ts。
- **ripples**: 中英文文章入口同步移除；旧链接进入既有 NotFound 页面；历史日志保留。当前工作分支已有其他未提交改动，本次只做本地移除，不打包发布。
- **verified**: 执行 npm run check 与构建产物残留检查，结果见本次任务输出。
- **keywords**: notes, remove, personal-brand-positioning, content-pillars-from-real-projects

### [E-2026-09-14-03] Wiki 总览回归 Eden 编辑式知识目录
- **type**: `design`
- **scope**: `/wiki`、Wiki 返回导航、双语静态首屏
- **impact**: Wiki 总览从 Poker 产品页视觉改为居中留白的知识目录；六篇笔记和本地 Skill 草稿继续可用。
- **changed**: `App.tsx` 改用简洁双语首屏、两栏开放条目、知识维护说明及 Notes/Projects 入口；`styles/pages/wiki.css` 独立管理 Wiki 布局并经 `index.css` 导入；同步 `seo-prerender.ts` 首屏与 `seo-routes.ts` 修改日期。
- **ripples**: Wiki 总览不再继承 Poker 页样式；全局菜单仍由 HeaderControls/shared.css 维护。Wiki 文章返回知识库，Poker 项目内的笔记卡保持原样。当前分支已有其他未提交工作，发布时不能把无关改动一起提交。
- **verified**: 执行 TypeScript 与 npm run check；本地检查 /wiki/、/zh/wiki/ 及 /wiki/skills/ 的链接、响应式布局和主题。具体结果见本次交付。
- **keywords**: Wiki, Eden, editorial, navigation, responsive, bilingual

## 2026-09-15

### [E-2026-09-15-01] Life OS 外链迁移到 Firebase Hosting 域名

- **type**: `chore`
- **scope**: `life-os`
- **impact**: `local`
- **changed**:
  - `App.tsx` `LifeOsFullPage` — `appUrl` 从 `https://edent95.github.io/LifeOs/` 改为 `https://life-os-eden95.web.app/`（主 CTA「Open Life OS / 打开 Life OS」与内嵌 iframe 共用），iframe 显示域名同步改为 `life-os-eden95.web.app`。
  - `seo-routes.ts` — `/life-os` 的 `dateModified` 与 `SITE_CONTENT_LASTMOD` 推到 `2026-09-15`。
- **ripples**:
  - **仓库里 Life OS 应用地址只剩这一处**（`App.tsx` 的 `appUrl` 常量 + 同组件内的 `domain` 显示字符串）；下次再迁域名改这两行即可，README / state / seo-static-content 都不写具体地址。
  - **旧的 GitHub Pages 地址 `edent95.github.io/LifeOs/` 不再被本站引用**；如果那边还在线，外部书签仍可用，但本站不会再指过去。
  - iframe 嵌入要求新域名允许被 `eden-tan.com` 嵌套（无 `X-Frame-Options: DENY` / 限制性 `frame-ancestors`）；Firebase Hosting 默认不加这些头，若日后加了，页面内预览会变空白但 CTA 不受影响。
- **verified**:
  - 全局 grep 无 `github.io/LifeOs` 残留。
  - `npm run check` 全绿。
  - 本地：`npm run dev` 后打开 `http://localhost:4180/life-os`，「Open Life OS」应指向 `https://life-os-eden95.web.app/`，预览框下方域名显示 `life-os-eden95.web.app`。
- **keywords**: Life OS, life-os-eden95.web.app, Firebase Hosting, 外链, appUrl, iframe domain, 域名迁移

## 2026-09-17

### [E-2026-09-17-01] 品牌指南加入「凹版线条语言」，并对齐已过时的首页媒体与动效规则
- **type**: `design`
- **scope**: `/brand-guide`、CSS art（新增 engraved-line 家族）
- **impact**: `module`
- **changed**:
  - `App.tsx` — 品牌指南新增第 05 节「凹版线条语言 / Engraved line language」：四张实时 CSS 样张（排线明暗阶、扭索纹玫瑰、凹版荷鲁斯之眼、钞票花边加黄金矩形构造线）与六条规则（明暗来自线的疏密、秩序来自扭索纹、一张图一种墨、黄金比例定位、画符号不画人、双线框）；后续章节顺延为 06–10。对齐过时内容：首页媒体规则由「6 个入口 / 4-2-1 栏 / 1480px」改为当前的 9 张 16:9 卡片两行（4 + 5）反向漂移、58 秒一轮、悬停与聚焦暂停、离开视口暂停视频；尺寸参考同步替换并加入黄金比例与排线线宽；首页用例、网格节奏同步；动效规则把「不做背景氛围动效」改为「环境动效只能慢、像机械」（允许拼贴漂移与 30 秒以上一圈的纹饰旋转，仍禁 glow / 扫描线 / card fade）；新增 CSS 规则「凹版线条用渐变画」、资产规则「插画默认用凹版语言」；表层系统分类加入「凹版线条语言」；原则「层级」改为「用尺寸、留白和线条疏密」；页脚对齐日期改为 2026-09-17、来源改为 logs/。
  - `components/css-art/index.tsx` — 新增 `EngravedHatchScale` / `EngravedRosette` / `EngravedEye` / `EngravedBorder`，六种墨色 tone（ink / rose / bronze / violet / teal / indigo）。
  - `styles/css-art/engraved-line.css`（新）— 样张实现；深色主题换深色版面、古铜金线；线条全部用渐变与 box-shadow；带 `prefers-reduced-motion`。`index.css` 引入。
  - `styles/pages/brand-guide.css` — 样张网格布局（2 / 1 栏）。
  - `AGENTS.md` — Story Style 引用的章节编号更新为 `09 / Story content`。
- **ripples**:
  - 样张组件未登记到 `css-art.registry.ts`（只在品牌指南使用）；若要在 `/project-css` 或首页复用，先登记并选类别。
  - 这套语言与 Life OS 应用内的插画 / 图标同源（Life OS 仓库 `src/styles/manual-scenes.css`、`css-icons.css`、`scripts/app-icon/`），两边的墨色与规则改动要一起看。
  - 本站 Life OS 产品页的 CSS 图标（`ProjectsLifeOsCssIcon`）仍是旧样式，Life OS 应用图标已改为凹版荷鲁斯之眼；产品页 FAQ 里「问 Agent / prompt 包」的说法也已与应用现状不符（应用已移除复制给 Agent 的入口），本次未改。
  - 品牌色板仍按本站 tokens（深色主色 Pink）；Life OS 应用深色主色已改为橙，两边不一致是既有状态。
- **verified**: `npx tsc --noEmit` 通过；`npm run check` 结果见本次交付；浏览器（Chrome，`npm run dev` → `http://localhost:4180/brand-guide`）桌面 1512px 浅色 / 深色看了第 05 节四张样张（修正了黄金螺旋弧不显示、荷鲁斯之眼螺旋与眼睛断开两处）；390px 同源 iframe `scrollWidth === clientWidth`，页面文字含「9 个真实入口」与新对齐日期、不含「6 个真实入口」。
- **keywords**: brand guide, 品牌指南, engraved, intaglio, money engraving, 凹版, 排线, hatching, guilloche, 扭索纹, 黄金比例, Eye of Horus, 首页拼贴, collage, motion rules

## 2026-09-18

### [E-2026-09-18-01] 新增 /project/miya：MiYa iOS 隐私政策兼技术支持页（中英双语、可预渲染）
- **type**: `feature`
- **scope**: `/project/miya`、`/project` app shelf、SEO prerender、CSS art（projects-icons 家族）
- **impact**: `module`
- **changed**:
  - `components/miya-privacy-content.ts`（新）— MiYa 隐私政策全文的中英结构化数据（收集 = 无、读取的健康数据表、存储位置、不会发生的事、你的控制权、非医疗产品、儿童、政策变更、联系与支持 d.tytern@gmail.com、开发者 Eden、生效日期 2026-09-05、适用 MiYa iOS 1.0），React 页与静态预渲染共用这一份。
  - `components/MiyaPrivacyPage.tsx`（新）— 懒加载页面组件；导航复用 `.project-home-nav` + `HeaderControls`（语言切换走站内 `/zh/` 路由），顶部「需要帮助？」锚点跳到醒目的联系卡。`styles/pages/miya-privacy.css`（新，已在 `index.css` 引入）— 760px 内容岛、MiYa 红色实色侧条与联系卡、表格横向滚动、手机宽度下流程图改两栏。
  - `App.tsx` — `pathWithoutBase === '/project/miya'` 分支；`/project` 货架加入第八个 app「MiYa」（`ProjectsMiyaCssIcon`）。
  - `components/css-art/index.tsx`、`styles/css-art/projects-icons.css`、`css-art.registry.ts` — 新增 `ProjectsMiyaCssIcon`（红底白心跳动 + 脉冲线，带 reduced-motion 与深色）。
  - `seo-routes.ts` — 登记 `/project/miya`（可索引、进 sitemap）；`/project` 的 `dateModified` 与 `SITE_CONTENT_LASTMOD` 推到 2026-09-18。`seo-static-content.ts` — `/project/miya` 静态正文由共享数据生成；`/project` 静态正文改为「八个 app」并提到 MiYa。`seo-prerender.ts` — `/project/*` 面包屑挂到「作品」下。`vite.config.ts` — 静态正文里的邮箱地址自动转成 `mailto:` 链接。
  - `README.md` — 路由表加入 `/project/miya`。
- **ripples**:
  - **MiYa 的 App Store Connect「Privacy Policy URL」与「Support URL」都应填 `https://eden-tan.com/project/miya/`**（中文版 `https://eden-tan.com/zh/project/miya/`）；以后改路径会让已上架 App 的隐私链接失效，别改。
  - 隐私政策内容改动只改 `components/miya-privacy-content.ts` 一处，React 页和静态 HTML 会一起变；实质性修改要同步改生效日期（数据里的 `meta`）并在 App 更新说明里注明，同时推 `/project/miya` 的 `dateModified`。
  - 这是站内第一个 `/project/<slug>` 嵌套路由：面包屑规则已加，但 `CLUSTERS`（相关页面）没收录它，相关链接走默认集群。
  - 与另一个未提交的工作分支 `work/20260918-split-app-routes`（把 App.tsx 拆到 `pages/`）会在 `App.tsx`、`logs/2026-09.md` 上冲突；后合并的一方要把 `/project/miya` 分支和货架条目搬进拆分后的文件。
  - `vite.config.ts` 的 `linkifyEmails` 作用于所有路由的静态正文段落；目前只有 MiYa 页含邮箱。
- **verified**: `npm run check` 全绿（verify → unit → typecheck → functions:check → harness build → smoke）；`dist/project/miya/index.html` 与 `dist/zh/project/miya/index.html` 静态正文含全部章节与 mailto 链接；本地 `vite preview --port 4917 --strictPort` 打开 `http://localhost:4917/project/miya/`（英文）与 `/zh/project/miya/`（中文）均正常渲染，结束后按端口停掉。
- **keywords**: MiYa, privacy policy, 隐私政策, support URL, 技术支持, App Store, HealthKit, /project/miya, prerender, 静态正文, ProjectsMiyaCssIcon

## 2026-09-22

### [E-2026-09-22-01] 首页 Life OS banner 换成新影片
- **type**: `content`
- **scope**: `public/home-banners/life-os-banner.mp4`、`public/home-banners/life-os-banner-poster.jpg`、`App.tsx`（首页 Life OS 卡片的 `imageAlt`）
- **impact**: `local` —— 只换首页 Life OS 卡片的视频素材与替代文字；文件名、卡片结构、链接、其他 banner 都不变。
- **changed**:
  - 用户提供的 `A_second_cinematic_continuo.mp4`（1280 × 720、H.264、24fps、10 秒、带 AAC 音轨、约 7.2MB）转成网页资源覆盖 `life-os-banner.mp4`：去掉音轨，libx264 `-crf 24 -preset slow`、yuv420p、24fps、`+faststart`，约 3.3MB（与其他首页 banner 同一量级）。
  - 从 0.5 秒抽一帧重新生成同尺寸 `life-os-banner-poster.jpg`（Reduce Motion / 加载前显示的静态图）。
  - 新画面是持杖斗篷行者在雨雾中直面巨龙、再举起一团光，中英文 `imageAlt` 随之改写。
- **ripples**:
  - 文件名沿用旧名，已访问过的浏览器 / service worker 可能短暂拿到旧的缓存视频；构建会刷新 `CACHE_NAME`，下次 SW 更新后即换新。
  - `public/marquee-probe.html` 引用同一路径，自动跟着换。
  - PR #30（App.tsx 拆到 `pages/`）合并前需把这条 `imageAlt` 同步到 `pages/HomePage.tsx`，否则会被拆分分支的旧文字覆盖。
- **verified**:
  - `ffprobe`：输出只有一条 h264 1280 × 720 yuv420p 24fps 视频流、10 秒、无音轨；poster 为 1280 × 720 JPEG。
  - `npm run check` 通过。
  - 本地：`npm run dev` 后打开 `http://localhost:4180/`，首页 Life OS 卡片播放新的巨龙雨雾影片、静音循环。
- **keywords**: Life OS, banner, 首页视频, life-os-banner.mp4, poster, imageAlt, 首页
