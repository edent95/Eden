# Change Log — 2026-08

### 2026-08-12 · PWA manifest 路由化（首页不再是 Conway）

- 类型：PWA / 站点身份修复 / vite plugin + public assets
- 改动：新增 `public/site.webmanifest`（`name: Eden Tan — Build systems from chaos.`、`short_name: Eden Tan`、`start_url: ./`、`theme_color: #1c1917`）与 `public/eden-app-icon.svg`（512 viewBox、maskable 安全区内的 E 标记）。
- 改动：重写 `vite.config.ts` 的 `web-app-manifest-with-base` 注入脚本，从「只有 /film-gallery 特判、其余一律 conway」改为三段路由映射：`/film-gallery` → film manifest、`/conways-game-of-life` → conway manifest、其余全部路由（含 `/`）→ site manifest；`theme-color` 与 `apple-touch-icon` 同步按路由切换。
- 改动：`public/sw.js` 的 `CACHE_NAME` 由 `eden-conway-v1` 升到 `eden-site-v2`，`APP_SHELL` 加入 `./site.webmanifest` 与 `./eden-app-icon.svg`。
- 改动：`README.md` 路由段落补充 manifest 路由映射说明。
- 原因：旧逻辑让首页及所有非 film 路由都挂 `conway.webmanifest`，其 `start_url` 为 `./conways-game-of-life`，导致从首页安装的 PWA 名字叫 Conway Life、图标是 Conway、打开直接进生命游戏，站点品牌身份被子项目覆盖。
- 影响：首页安装出来的 app 现在是 Eden Tan 且 `start_url` 回到 `./`；Conway 与 Film Gallery 仍各自保留独立可安装身份；页面路由与 UI 无任何视觉改动。
- 验证：`npx tsc --noEmit` 通过；`npx vite build --outDir dist_manifest_v2` 通过（✓ 2085 modules transformed，✓ built in 3.27s），产物 index.html 已注入三段映射脚本，`site.webmanifest` / `eden-app-icon.svg` 正确复制到产物根目录，`sw.js` 已带 `eden-site-v2`；node 模拟 9 条路径分流结果正确（`/` `/notes` `/poker` `/life-os` `/cellular-automata-lab` → site，`/conways-game-of-life[/]` → conway，`/film-gallery[/]` → film）。临时产物目录已移入 `_to_delete/dist_manifest_v2`（本环境无法直接删除文件）。
- 注意：`dist/` 因 `.DS_Store` 权限问题无法在此环境重建，正式发布前需在本机跑一次 `npm run build`。
- 后续：本机已安装的旧「Conway Life」PWA 需卸载后重新安装才能拿到新身份；浏览器端如仍看到旧 manifest，在 DevTools → Application 里 Unregister service worker 并清 cache storage。

## 2026-08-13 — 新增 /penneys-game（Penney's Game 硬币骗局）

**做了什么**
- 新路由 `/penneys-game`：闯关 + 筹码对赌混合玩法，中英双语。
  - 战役 5 关：街头热身 → 被宰（你先选，庄家最优应对）→ 反杀（庄家先选，你要选最优）→ 四位大师（4 位序列）→ 通杀（3/4 位混合，要选对且赢）。每关有筹码下注、通关奖励与"这一关教会你什么"的结算文案。
  - 排位「盲选大师赛」：通关第 4 关解锁，限时选出最优应对，错一次结束，每 3 题时间收紧 1 秒；成绩上全球排行榜。
  - 实验室：通关第 3 关解锁，任意对局 + 1k/10k/100k 蒙特卡洛模拟，用来验证实测胜率收敛到精确概率。
- `services/penneyGame.ts`：纯逻辑层。胜率用 Conway leading-numbers 公式精确计算（不是模拟），已与 20 万局蒙特卡洛交叉验证；口诀「对手 ABC → 选 (notB)+A+B」经暴力搜索确认对全部 8 个三位序列都是最优解。
- `services/penneyLeaderboard.ts`：Firebase RTDB **REST** 客户端（匿名登录 + 刷新 token + 读写），**没有引入 firebase npm 包**，主 bundle 零增重。任何网络失败都降级为 localStorage 本地榜并在 UI 标注。
- `components/PenneysGamePage.tsx` + `styles/pages/penneys-game.css` + `styles/css-art/penney-coins.css`（硬币物件与 app icon，已登记进 `css-art.registry.ts`，组件从 `components/css-art` 导出）。
- 路由注册：`seo-routes.ts`（中英 title/desc，进 sitemap）、`App.tsx`（href / 路由分支 / `/project` app 货架新增一格）、`index.css` 两个 import、README 路由段。

**为什么**
- 用户要把之前的 Penney's Game 原型变成站上真正能玩的游戏，并且要闯关 + 对赌 + 全球排行榜。
- 选 REST 而不是 Firebase SDK：静态站为一个路由背 ~100 kB gzip 不划算。

**影响**
- `npx tsc --noEmit` 通过；`vite build` 通过（2089 modules）。sitemap 已含 `https://edentan.site/penneys-game`。
- `npm run build` 在本机会因为 `dist/.DS_Store` 删除权限报 EPERM（环境问题，不是代码问题）；用 `--outDir` 验证构建可绕开。

**下一步**
- 排行榜要真正跑起来，需要手动部署一次数据库规则：见 `docs/penney-leaderboard.md`，在 `poker-power-card` 里加 `penneyLeaderboard` 节点后 `firebase deploy --only database`。规则没部署之前页面会自动显示"本地榜"，不会报错。

## 2026-08-13 — 产品页统一与本次生产发布

**做了什么**
- 新增可复用的 `ProductStorePage`，将 Jiju、ETReportHub、Friday Poker Club、Film Gallery 与 Life OS 收敛到同一套产品页结构、导航、FAQ、资料与 related products 呈现。
- 新增 `/project` app shelf，并补齐 Life OS / Penney's Game CSS app icon、首页 Life OS 动态 banner 及对应 route/page styles；旧的独立 `life-os.css` 已由共享产品页样式替代。
- 扩大临时构建目录忽略规则，避免 Tailwind v4 扫描遗留验证产物，并让 Vite dev watcher 同步忽略这些目录。

**为什么**
- 让多个产品详情页使用一致、可维护的产品展示骨架，同时把新增游戏与现有项目纳入同一发现入口。
- 避免本地验证产物干扰开发服务器和 Tailwind source detection。

**影响与验证**
- 本次完整工作区将直接提交并推送 `main`，由 GitHub Pages workflow 发布到 `https://edentan.site`。
- `npm run check` 与 `git diff --check` 通过（TypeScript 无错误，Vite ✓ 2089 modules transformed，✓ built in 1.20s）。

**下一步**
- 发布后检查 GitHub Pages workflow 与首页、`/project`、`/penneys-game`、`/life-os`；Penney 全球榜仍需按 `docs/penney-leaderboard.md` 单独部署 Firebase database rules。

## 2026-08-13 — 首页 Film Gallery banner 换成实拍影片

**做了什么**
- 将首页 `Film Gallery` 卡片从原本的 CSS film-strip 动画改成用户提供的 10 秒 16:9 双反相机实拍影片。
- 新增 `public/home-banners/film-gallery-banner.mp4` 与静态 poster；首页继续使用现有的视口播放、静音循环、Safari inline autoplay 与 Reduce Motion fallback。
- 补齐中英文画面替代文字；Film Gallery 页面、路由、CTA 与原有 CSS art app icon 保持不变。

**为什么**
- 用户希望以新的 `sec_video.mp4` 取代首页 Film Gallery banner，同时保持现有卡片交互与可访问性。

**影响**
- 只改变首页 Film Gallery 卡片的视觉素材；其他首页 banner 与 Film Gallery 产品页不变。
- `npm run build`、`npm run typecheck` 与 `git diff --check` 通过（Vite ✓ 2089 modules transformed，✓ built in 1.16s）；生产包已包含新 MP4 与 poster。

**下一步**
- 无。

## 2026-08-14 — 修复 iPhone banner 停止与异常背景

**做了什么**
- 将 marquee 的 hover / focus 暂停逻辑限制在真正支持 hover 的 fine-pointer 设备；触控设备强制维持 `animation-play-state: running`，避免 iOS Safari sticky hover 导致两排永久停住。
- 移除 mobile 的 `padding + clip-path` 合成裁切层，改为真正 full-bleed 的透明 row；卡宽按 viewport 三等分计算。
- Mobile 不再渲染只为 desktop hover CTA 服务的暗色渐层覆盖，保留原始 banner 画面颜色。

**为什么**
- iOS 会在触控后保留模拟 hover / focus 状态，原规则会持续暂停 track；同时 clip-path 与 transform 合成会产生不必要的黑色背景区与裁切接缝。

**影响**
- 手机 banner 与 desktop 一样持续左右移动，并直接铺到 viewport 两侧；卡片画面不再被额外压暗。Desktop hover 暂停与 CTA 效果不变。
- `npm run check` 的 unit、TypeScript、production build 与 built-site smoke 全部通过（Vite ✓ 2089 modules transformed，✓ built in 1.23s）；本次文件的 `git diff --check` 通过。Playwright 在 390 × 844 viewport 实测两排均为 390px full-width、透明背景、无 clip / mask、卡宽 126px、overlay 不渲染，页面 `scrollWidth` 为 390px；一秒前后两排 transform 持续变化且 animation state 均为 running，console 0 error。
- 全工作区 `git diff --check` 另被并行 harness 改动中 `README.md` 第 93–94 行的既有行尾空格挡住；本次未修改该组文件。

**下一步**
- 无。

## 2026-08-14 — 修复首页 mobile banner 并同步 desktop 动效

**做了什么**
- 手机端继续使用与 desktop 相同的双行 marquee：上排向左、下排向右循环移动，不再降级成静态网格。
- 每张卡片按一屏三张等比例缩放，使用 16px 左右安全留白、6px gap 与 8px 圆角；mobile 动画时长调整为 28 / 32 秒，保持接近 desktop 的移动观感。

**为什么**
- 原 mobile `46vw` 卡宽只显示约两张半，右侧卡片看起来像意外被截断；用户希望手机保留 desktop 的完整动态 banner 体验。

**影响**
- 手机端卡片按一屏三张的宽度缩放，并保留两排相反方向的自动滚动；边缘卡片像 desktop marquee 一样自然进入与离开画面。Film Gallery 新视频会随下排正常进入可视区；desktop 与 tablet 不变。
- `npm run build`、`npm run typecheck` 与 `git diff --check` 通过（Vite ✓ 2089 modules transformed，✓ built in 1.18s）。Playwright 在 390 × 844 viewport 实测卡宽约 115.33px、两排动画分别为 28 / 32 秒、transform 持续变化，`scrollWidth` 与 viewport 同为 390px；Film Gallery 进入画面后正常播放，console 0 error。

**下一步**
- 无。

## 2026-08-14 00:54:26 +08 — Eden executable harness phase 1

- 类型：流程 / CI / 测试 / 文档
- 改动：新增 `harness.config.json`、`state/current.md`、`scripts/harness/*`、`scripts/wiki/lint.mjs`、Node 原生单元测试与 Pull Request `verify.yml`；`npm run check` 现在统一执行 skill、route、log、Wiki、CSS policy、Penney/PWA unit、TypeScript、production build 与 built-site smoke checks。部署 workflow 升级为 Node 22，并在上传 GitHub Pages artifact 前运行同一闸门。
- 改动：修复 `AGENTS.md` / `soul.md` 中四个不存在的本地 skill 路径；三种沟通策略以内嵌规则为事实源，Apple interface reference 指向实际存在的 `.agents/skills/apple-design/SKILL.md`。补齐 README 缺失的 `/project`、`/jiju-revamp` 和真实 workflow 路径。
- 改动：把 PWA manifest 路由选择抽成可测试的 `pwa-manifests.ts`，保持 Film Gallery、Conway 与默认 Eden manifest 的既有公开行为。
- 原因：现有规则主要依赖 Agent 自觉遵守，且 skill、route、README、CI 已出现事实漂移；需要把客观规则变成失败时会阻止 PR 或部署的程序。
- 影响：普通任务以 `npm run check` 作为单一 Done gate；PR 与 main 部署使用同一套事实检查。`log 2.md` 与 `soul 2.md` 暂保留为明确标注的 legacy snapshots，没有执行不可逆历史清理。Wiki Markdown 迁移和大型日志分档留待兼容性更充分的第二阶段。执行期间出现的并行 `styles/pages/home.css` mobile banner 修改不属于本 harness 改动，已原样保留且仍通过完整检查。
- 验证：完整 `npm run check` 通过：5 组 policy gates、7 个 Node unit tests、TypeScript、Vite production build（✓ 2089 modules）与 built-site smoke checks 全部通过；production artifacts、assets、三个 manifests、GitHub Pages redirect 与 37 条 sitemap route 均通过检查。`git diff --check` 通过。
- 后续：在 GitHub branch protection 中将 `Eden Harness / verify` 设为 required check；随后分阶段迁移 Wiki/Notes Markdown 与月度日志索引。

## 2026-08-14 01:12:00 +08 — Markdown knowledge source and monthly log archive

- 类型：结构 / LLM Wiki / 日志 / Harness
- 改动：将 `App.tsx` 内 6 个 Wiki entries 与 9 篇双语 Notes 无损迁移到 `wiki/pages/*.md` 和 `wiki/essays/*.md`；每篇包含可 lint 的 frontmatter 与结构化 JSON payload，`scripts/wiki/build.mjs` 将它们编译成 tracked `generated/content.ts`，网站继续使用相同的数据结构与公开 URL。
- 改动：增强 Wiki lint，检查 ID、type、route、order、双语字段、payload slug、引用编号及 `[[note:...]]` 内链；新增 `raw/README.md` 作为不可变 source ingest 边界，并保留 `public/` 原始全文 URL 的现有兼容性。
- 改动：将原 4,000 多行 `log.md` 的 469 条记录无损分拆为 `logs/2026-04.md` 至 `logs/2026-08.md`，`log.md` 改为稳定兼容入口；新增自动生成的 `logs/index.md` recent index 及 log freshness gate。
- 原因：减少 Agent 每次启动需要吞入的上下文，并让知识内容、引用、路线和历史记录从大型代码/单体日志变成可独立检索、验证和编译的项目资产。
- 影响：`App.tsx` 从 11,158 行降到约 10,229 行；内容编辑流程变成 Markdown → `npm run wiki:build` → `npm run check`，日志流程变成月度文件 → `npm run log:index` → `npm run check`。历史条目没有删除或改写，legacy `log 2.md` / `soul 2.md` 仍保持不动。GitHub `main` branch protection 已启用：required `verify`、strict up-to-date、管理员也受保护、必须解决 review conversations，并禁止 force-push 与 branch deletion。
- 验证：完整 `npm run check` 通过：37 条 route contracts、470 条日志 index freshness、16 个 Wiki Markdown 文件、22 条内链、生成文件一致性、12 个 CSS art families、7 个 unit tests、TypeScript、production build（✓ 2090 modules）及 built-site smoke 全部通过。迁移审计逐对象比较旧 `App.tsx` 与 Markdown payload，6 个 Wiki + 9 篇 Notes 的内容和顺序完全一致；旧 `log.md` 的 469 个历史 block hash 全部存在于月度档案。`git diff --check` 通过。
- 后续：保护新工作流稳定后，再按 route family 拆分 `App.tsx` 组件；不在同一阶段同时做内容迁移和 UI 重构。首次使用新保护规则时通过 PR 合并，不直接推送 `main`。

## 2026-08-14 01:14:26 +08 — Protected operator workflow harness phase 3

- 类型：流程 / GitHub / 发布 / Harness
- 改动：新增 `npm run task:new -- "任务名"`，从默认分支建立带 Kuala Lumpur 时间戳的 `work/*` 分支；若工作误留在 `main`，不会 reset 或丢弃，而是在发布前安全转移到工作分支。
- 改动：新增 `npm run ready`，按顺序执行 Wiki 编译、日志索引和完整 `npm run check`；新增 `npm run publish -- "标题"`，串联范围预览、确认、提交、push、PR、required checks、squash merge、Pages deploy 与线上首页 / sitemap / manifest 检查。
- 改动：发布命令支持无写入的 `--dry-run`、显式非交互确认 `--yes` 和保留已验证 PR 的 `--no-merge`；新增 workflow utility 单元测试及 `docs/operator-workflow.md`，并同步 README、AGENTS 与 current state。
- 原因：branch protection 已经能阻止错误直推，但原流程仍要求用户手动记住建分支、生成文件、跑检查、建 PR、等待 CI、合并和部署；第三阶段让正确且安全的路径成为最短路径。
- 影响：日常任务只需记住 `task:new` 和 `publish`，Markdown 规则通过脚本、测试、GitHub 权限及自动反馈形成闭环；`main` 的 required `verify`、管理员保护、禁止 force-push 与禁止删除保持不变。
- 验证：workflow utility 的 5 个定向单元测试和 `publish --dry-run` 已通过；dry-run 确认只读取当前分支与文件范围，没有生成文件、提交、push 或 GitHub 写操作。完整 `npm run ready` 通过：全部 12 个单元测试、TypeScript、Vite production build（✓ 2090 modules）与 built-site smoke checks 均通过；真实 PR、deploy 与 live check 由本阶段自己的 `publish` 流程继续验证。
- 后续：从真实任务的失败案例补回归测试；若未来加入 preview environment，应接在 PR verify 后、merge 前，不降低现有保护等级。

## 2026-08-14 01:19:00 +08 — Visual Harness operator menu

- 类型：文档 / 操作界面 / Harness / HTML
- 改动：新增独立的 `public/operator-menu.html` 图形化操作菜单，用发布流水线、五步闭环、三张操作卡、安全盾牌、故障恢复卡和快速菜单解释 `task:new`、`ready` 与 `publish`；三条核心命令支持一键复制，并适配手机横向流程浏览、键盘焦点和 reduced motion。
- 改动：README 与 operator workflow 文档增加本地和线上入口；built-site smoke check 现在要求 production artifact 必须包含该 HTML，并检查三条核心命令没有丢失。
- 原因：完整闭环已经可执行，但纯 Markdown 对第一次使用的人仍偏抽象；需要一个图形更多、可扫描、可直接复制命令的 user menu，降低学习成本。
- 影响：用户无需先读完整工程文档，就能从 `/operator-menu.html` 理解正常路径、安全边界和恢复动作；页面只解释和复制，不在浏览器中执行 shell，也不改变 branch protection。
- 验证：Playwright 在 1440 × 1000 与 390 × 844 viewport 实测页面结构和视觉层级正常，两种尺寸的 `documentElement.scrollWidth` 均与 viewport 一致，没有页面级横向溢出；复制按钮成功进入 active 状态并显示反馈，补齐 SVG favicon 后 console 为 0 error / 0 warning。`npm run ready` 将继续确认 HTML 被 Vite 复制到 production artifact，并由 smoke contract 检查三条核心命令。
- 后续：若未来做桌面控制台，可在保持终端确认和 GitHub 权限边界的前提下，把此页作为只读 UI 原型，不应让浏览器直接持有仓库写权限。

## 2026-08-14 03:12:26 +08 — PWA cache consistency and build visibility

- 类型：PWA / Service Worker / 主题 / 可观测性
- 改动：Service Worker 只对带内容 hash 的同源 assets 使用 cache-first；导航请求优先网络且不再把跨版本 HTML 写回运行时缓存，离线时仅回退到当前 worker 安装的 app shell。
- 改动：构建时生成统一 `BUILD_ID`，同时写入 Service Worker cache name 与前端 `data-build` / console；新增全局类型声明，方便确认 PWA 实际运行的部署版本。
- 改动：前端根据当前页面背景同步 `theme-color` meta，并在 `data-theme` 变化时更新，避免 iOS standalone 状态栏在浅色模式仍显示深色条。
- 原因：已安装 PWA 可能长期保留旧页面，并把旧 HTML 与新资源混用；同时缺少可见 build 标识，难以快速判断当前运行版本，静态 theme color 也会造成 iOS 状态栏与页面主题不一致。
- 影响：在线导航会获取当前部署 HTML，hashed assets 仍可高效缓存；离线 fallback 与 cache version 保持同一 build。主题切换后的状态栏颜色更贴合页面，排查陈旧 PWA 时可直接读取 `<html data-build>` 或控制台 build id。
- 验证：由 `npm run publish` 执行 Wiki / 日志生成、完整 `npm run check`、PR required check、部署与线上检查。
- 后续：无。

## 2026-08-14 15:33:14 +0800 — iOS marquee drift distance

- 类型：首页 / 动画 / WebKit / 诊断
- 改动：首页 collage marquee 的位移距离不再写成 `translateX(-50%)`。`HomeCollageRow` 在 `useLayoutEffect` 中量出单个 run 的实际宽度写入 `--eden-run-shift`，并以 `ResizeObserver` 跟随断点变化重新测量；两个 keyframes 改用 `translateX(calc(-1 * var(--eden-run-shift, 50%)))`，保留 50% 作为测量未执行时的回退。
- 改动：新增 `public/marquee-probe.html`（noindex）诊断页，把 marquee 配方拆成五个每次只改一个变量的变体，实时输出 `translateX`、distinct 取值数、animation 状态、媒体查询结果与 rAF 帧数，供真机排查使用。
- 原因：iOS Safari 对 `width: max-content` 的 flex 轨道解析百分比位移时参照盒错误，`-50%` 被算成约 25px 而非 532px。首页 28s 时长下等于约 0.9px/s，肉眼完全静止；Chromium 按规范解析，所以桌面端的手机模拟一直正常，掩盖了这个问题。
- 影响：真机 iOS 上 marquee 恢复正常漂移，桌面端行为不变——测得 `--eden-run-shift` 为 531.9375px，正好是 1064px 轨道的一半，与原 `-50%` 数值等价。诊断页为独立静态文件，不进入 SPA 路由，不影响既有页面。
- 验证：iPhone（iOS 18.7 / WebKit 605.1.15）实测五个变体，使用百分比的四行 distinct 全部锁死在 25，改用像素的一行达到 600；`prefers-reduced-motion` 为 no-preference、`animationPlayState` 为 running，排除无障碍设置与动画未启动。Chromium iPhone 15 viewport 复测修复后两行分别 distinct=40，方向正确。本地 `npm run verify`、12 个 unit tests、typecheck、production build 与 built-site smoke 全部通过。
- 后续：卡片内 video 是否被祖先 `overflow: hidden` 与 `border-radius` 正确裁剪尚未确认，如真机仍有溢出，另做一轮针对裁剪的探针，不与本次位移问题混在一起处理。

## 2026-08-14 15:56:21 +0800 — Static mobile collage grid

- 类型：首页 / 响应式 / 动画取舍
- 改动：`max-width: 600px` 下首页 collage 从跑马灯改为静态 2 行 × 4 列网格。`.eden-collage-row / -track / -run` 用 `display: contents` 折叠，卡片直接成为 `.eden-collage` 的 grid item；仅为无缝循环存在的重复 run（`aria-hidden="true"`）隐藏，轨道动画显式设为 `none`。
- 改动：移除该断点下的 `flex-basis: calc((100vw - 12px) / 3)` 与 animation-duration 覆盖，卡片改为 `width: 100%` 填满栅格列。桌面端跑马灯与 `--eden-run-shift` 像素测量逻辑保持不变。
- 原因：手机宽度下单张卡片仅约 86px，漂移带来的信息量低于视觉噪音；同时 iOS Safari 对该轨道的动画解析本就不可靠，与其继续追查 video 层裁剪问题，不如在这个断点取消动画。
- 影响：393px 下容器 361px、四列各 85.75px、卡片 86×48，8 张卡排满两行；320px 下四列各 67.5px、卡片 68×38。两种 viewport 的 `documentElement.scrollWidth` 均等于 viewport，无页面级横向溢出。手机端不再有自动播放的横向动画，可点击行为不变。
- 验证：Chromium iPhone 15 与 iPhone SE viewport 实测栅格列数、卡片尺寸、可见卡片数（8，重复 run 已隐藏）、`animationName: none` 与零溢出，并截图确认两行四列。完整 `npm run check`（含 `HARNESS_BASE_REF`）通过。
- 后续：video 是否被祖先 `overflow: hidden` 与 `border-radius` 正确裁剪仍未定论，但手机端已不依赖动画，优先级下降；若桌面端出现同类溢出再单独排查。

## 2026-08-14 17:00:00 +0800 — 首页新增可玩 Penney's Game 区块

- 类型：首页 / 互动区块 / 逻辑复用
- 改动：新增 `components/HomePenneyGame.tsx`，插在首页 `.eden-collage-section` 之后、`01 · About Eden` 之前，`id="penney"`。玩法是 `/penneys-game` 的最小切片：固定 3 位序列、你永远先选、庄家永远用 `bestResponse` 最优应对，展示庄家胜率（百分比 + `formatOdds` 比值）、逐枚落下的翻币流、结算时用 mint / amber 描边圈出获胜窗口，并累计本次会话的比分。
- 改动：概率与回合逻辑全部从 `services/penneyGame.ts` 复用（`allSequences` / `bestResponse` / `resolveRound` / `winProbability` / `formatOdds`），组件只负责呈现；**刻意不读写 `PENNEY_SAVE_KEY`**，首页试玩不会污染完整版的 localStorage 存档。
- 改动：`styles/pages/home.css` 追加 `.eden-penney-*` 版面样式（仅 layout，硬币物件与落币动效沿用 `styles/css-art/penney-coins.css`），沿用 `.eden-home` 的 ink/paper/panel/blue/orange 变量、28px 卡片圆角、112px / 90px 段落留白；900px 转单列、600px 选择器转 2 列。中文标题另加 `.is-zh` 放宽字距，避免 `-.055em` 的 Latin display 字距压到全角字。
- 原因：用户要把 poker-power-card 里的 Penney's Game 放到首页。站上其实已有完整 `/penneys-game` 路由，所以首页不重复实现游戏，而是做一个「当面演示骗局」的可玩证据块——它正好承接 About 段落「一旦看见系统，你就能改变它」的论点，也符合 AGENTS.md 里首页要有可玩/可读证据的方向。
- 影响：首页在拼贴与 About 之间多一屏内容；`/penneys-game` 路由、完整版组件与存档均未改动。主 bundle 由 464.85 kB 增至 464.87 kB（gzip 160.57 → 160.58 kB），CSS 352.92 → 352.98 kB，增量可忽略（逻辑本就已在 bundle 内）。
- 验证：`npm run typecheck` 通过；`npx vite build --outDir dist_penney_check2` 通过（✓ 2091 modules transformed，✓ built in 3.40s）；`npm run verify:css`、`verify:routes`、`verify:skills`、`verify:wiki` 通过；12 个 unit tests 通过。Chromium 实测桌面 1440 / 手机 390 / light / dark（`data-theme="dark"`）/ 中英双语 / `prefers-reduced-motion: reduce` 六种组合：一局完整走完选择→庄家应对→翻币→结算，reduced-motion 下直接跳到结果且比分正确累加，庄家胜率显示 67%（2:1）与 75%（3:1）与精确公式一致。
- 后续：`dist/` 仍因 `.DS_Store` 权限无法在此环境重建，正式发布前需在本机跑一次 `npm run build`；临时产物 `dist_penney_check` / `dist_penney_check2` 已移入 `_to_delete/`（本环境无法直接删除文件），可自行清掉。

## 2026-08-14 17:24:00 +0800 — 首页 Penney 区块改为每日 IP Mini Coin Slot 擂台

- 类型：首页 / Mini game / Firebase Functions / 排行榜
- 改动：重做 `HomePenneyGame` 的信息与玩法，不再出现「你先选就输」、最优应对、胜率公式或赔率；首页现在以 Mini Coin Slot 呈现三枚硬币票、庄家隐藏票、逐枚 spin、个人 credits / wins / plays / win rate，以及满 10 局才入榜的访客 Top 5。
- 改动：新增 Firebase Functions v2 `penneyMiniApi`（Node.js 22 / `asia-southeast1`）。一局一 credit，每个 IP 按 `Asia/Kuala_Lumpur` 日期每天最多 100 局；原始 IP 只在请求内使用，以 Secret Manager 的 `PENNEY_IP_SALT` 做 HMAC-SHA256 后才成为 RTDB key，不存储明文 IP。庄家票面、随机翻币、胜负与积分扣减全部在服务端产生，并用单个 RTDB transaction 原子更新。
- 改动：新增 `services/penneyMini.ts` typed API client、Functions package / lockfile、4 个 quota / day boundary / server outcome / ranking 单测、`docs/penney-mini-arena.md`，并把 Functions syntax check 接入 `npm run check`。排行榜按胜率、胜场、局数排序，至少 10 局才进入正式名次；同一 NAT 下的家庭或办公室按用户要求共享 quota 与记录。
- 原因：用户希望访客通过游戏自己发现非传递规律，并用胜率辨认「同道中人」，而不是首页直接把规律告诉所有人；同时需要真实可竞争的每日额度和排行榜，不能用可清除的 localStorage 冒充 IP 限额。
- 影响：Firebase 项目 `poker-power-card-3abea` 首次启用 Cloud Functions / Cloud Run / Eventarc，生产函数已部署到 `penneyMiniApi`；新增私有 RTDB node `penneyMiniPlayers`，现有 poker 数据与旧 `penneyLeaderboard` 均未改动。测试 POST 产生的一条 `__qa__` 记录已精确删除，生产榜保持为空。
- 验证：Functions GET 返回 CORS `https://edentan.site`、新 IP 100 credits 与 Malaysia reset 信息；生产 POST 从 100 扣到 99，并返回服务端 house ticket / flip stream / winner，RTDB 原子记录正确。完整 `npm run check` 通过：16 个 unit tests、TypeScript、Functions syntax、2092 modules production build 与 built-site smoke 全部成功。Playwright 在 `http://127.0.0.1:4180/#penney` 实测 0 console errors：API 初始 100 / 100、spin 后变 99 / 100，plays 0→1、庄家票面与逐枚结果出现、入榜提示 10→9；QA row 随后从生产 RTDB 精确删除，节点恢复 `null`。
- 后续：Firebase Admin 当前依赖树经未使用的 Cloud Storage 路径带有 `uuid` moderate advisory；mini API 不调用 UUID / Storage，待上游提供兼容升级后复查，不按 audit 的错误建议降级 Admin SDK。

## 2026-08-19 14:52:12 +0800 — 主域名 OG 与 Mini Coin Slot CORS 迁移

- 类型：域名迁移 / SEO / Firebase Functions / CORS
- 改动：将 GitHub Pages 生产域名事实源从 `edentan.site` 更新为 `eden-tan.com`，同步 `public/CNAME`、production build `VITE_SITE_URL`、publish live-check 默认域名、sitemap smoke contract、README 与 current state；新增 built `index.html` 的 OG image 必须使用 `https://eden-tan.com/og-image.jpg` 的回归检查。
- 改动：`penneyMiniApi` CORS allowlist 新增 `https://eden-tan.com` 与 `https://www.eden-tan.com`，迁移期间保留旧域名 origin，并同步 Mini Coin Slot 运维文档与验证命令。
- 原因：GitHub Pages 已绑定 `eden-tan.com`，但构建仍使用旧 `VITE_SITE_URL`，导致线上 OG 图片绝对地址指向旧域名；生产 Functions 也只允许旧 origin，新域名的浏览器 preflight 无 `Access-Control-Allow-Origin`。
- 影响：下次 Pages 部署后的 OG、Twitter image、sitemap 与 robots 统一使用新主域名；Functions 部署后首页 Mini Coin Slot 可从新域名正常请求 API。旧邮箱 `hello@edentan.site` 与尚未完成迁移的 `poker.edentan.site` 链接不在本次范围。
- 验证：`npm run check` 通过：37 条 route contracts、478 条日志索引、16 个单元测试、TypeScript、Functions syntax、2092 modules production build 与 built-site smoke 全部成功；产物 OG image 与 sitemap 已通过 `eden-tan.com` 断言。受保护 publish、Pages 线上 OG 与 Functions CORS preflight 由本任务继续读回验证。
- 后续：`poker.eden-tan.com` 连接完成后，再独立更新 Eden 主站中的 Poker 外链；新邮箱可用前保留旧邮箱地址。

## 2026-08-19 15:14:15 +0800 — Eden Firebase 独立项目与数据边界

- 类型：Firebase / 数据迁移 / 项目边界
- 改动：新建独立 Firebase 项目 `eden-tan`、Web App 与 Singapore Realtime Database，启用 Anonymous Authentication，并把 Penney 完整版排行榜规则纳入本仓库的 `database.rules.json` 与部署配置。前端 API key、Database URL、Mini Slot Functions URL、Functions Admin Database URL 和部署脚本全部改指 `eden-tan`。
- 改动：把旧 Poker Firebase 中现存的 2 条 `penneyMiniPlayers` 累计记录复制到 `eden-tan`；新增 `verify:firebase` harness，确保活跃配置不会重新指向 `poker-power-card-3abea`，并把「Eden 后端不得复用 sibling product 项目」写入 `AGENTS.md`、`soul.md`、运维文档和 current state。
- 原因：用户明确要求 `eden-tan.com` 的 Firebase、排行榜与访客数据独立，不要继续和 Poker 产品项目混在一起。
- 影响：新的数据库规则已在 `eden-tan-default-rtdb` 发布，完整版排行榜可使用独立匿名身份，Mini Slot 的既有记录已保留；旧 Poker 项目的 Function 与数据暂时不删除，确保迁移完成前线上游戏仍可用。本分支尚未发布，因此生产网站仍指向旧端点。
- 验证：Firebase CLI 确认数据库规则语法有效并成功发布，目标 `penneyMiniPlayers` 浅读回为 2 条；Firebase Console 显示 Anonymous provider 为 Enabled。Secret Manager 明确返回 `eden-tan` 当前 Spark 方案无法启用，Functions 部署前必须由用户确认升级 Blaze。
- 后续：用户确认 Blaze 后，复制 `PENNEY_IP_SALT`、部署 `penneyMiniApi`、验证新端点与排行榜，再走受保护 publish 发布网站；线上读回成功后精确清理旧 Poker 项目的 Eden Function、Secret 与两个数据库节点。

## 2026-08-19 15:17:00 +0800 — Poker 外链迁移到新子域名

- 类型：域名迁移 / Poker / 外链
- 改动：将首页项目入口、Poker 详情页、iframe stage、安装链接与中英文 CTA 中的 `poker.edentan.site` 统一更新为 `poker.eden-tan.com`。
- 原因：新 Poker 子域名已完成 Firebase App Hosting 连接与 TLS 签发，主站不应继续把访客导向旧子域名。
- 影响：Eden 主站所有 Poker 入口会直接打开新域名；旧邮箱 `hello@edentan.site` 保持不变，旧 Poker DNS 记录也不在本次仓库改动范围内。
- 验证：运行 `npm run check`，确认路由、类型检查、production build 与 built-site smoke checks 全部通过；发布后读回主站构建与新 Poker HTTPS。
- 后续：旧 `poker.edentan.site` 在 Firebase 侧保留期间设置 301 到 `poker.eden-tan.com`，并维持旧 DNS 记录以承接历史链接。

## 2026-08-19 15:44:00 +0800 — 为每个公开 route 生成真实 HTML 目录

- 类型：SEO / GitHub Pages / Static route HTML
- 改动：production build 现在读取 `seo-routes.ts`，为全部 37 条已登记 route 输出真实的 `<route>/index.html`；每份 HTML 在服务器响应里直接带有该 route 的英文 title、description、robots、canonical 与 `og:url`，隐藏 route 继续输出 `noindex, follow`。
- 改动：sitemap、静态 HTML 与客户端 SEO 统一使用非首页带尾斜线的 canonical URL；built-site smoke 新增逐 route artifact 与 metadata 检查，`public/404.html` 只继续承接 registry 之外的未知路径。
- 原因：GitHub Pages 原本只有根 `index.html`，sitemap 中 32 条深层 URL 会先返回 GitHub 404，再靠 SPA 脚本恢复；Google Search Console 因而把 `/etreporthub`、`/poker`、`/projects` 等页面判为 404，无法稳定索引。
- 影响：`/poker/`、`/wiki/vite/`、`/notes/.../` 等 canonical 深链可由纯静态主机直接返回 `200` 和 route-specific SEO metadata；无尾斜线 URL 只做一次目录规范化跳转，sitemap 不再提交会跳转的 URL。
- 验证：production build 生成 37 份 route `index.html`，built-site smoke 全部通过；纯静态服务器实测 `/poker` → `301 /poker/`、`/poker/` → `200`、`/wiki/vite/` → `200`、noindex 的 `/life/` → `200`，未登记的 `/not-registered/` 保持 `404`。完整 `npm run check` 与线上 GitHub Pages 状态由本任务发布前后继续验证。
- 后续：部署后读回全部 sitemap URL 的最终 HTTP 状态，并让 Google Search Console 重新处理更新后的 sitemap；历史 404 报告需要等待 Google 重新抓取后消退。

## 2026-08-19 15:45:40 +0800 — Blaze 启用与 Functions 首次部署诊断

- 类型：Firebase / Cloud Functions / Artifact Registry
- 改动：确认 `eden-tan` 已升级 Blaze，把旧项目的 `PENNEY_IP_SALT` 规范化复制到新 Secret Manager 并以长度与哈希比对确认 64 字节值一致；验证 Anonymous Auth、排行榜合法写入、低分覆盖拒绝、公开读取与测试资料清理均正常。
- 改动：为首次 Functions 部署启用 Cloud Functions、Cloud Build、Cloud Run、Eventarc、Artifact Registry 与 Secret Manager API，并显式创建 `service-1092238962184@gcp-sa-artifactregistry.iam.gserviceaccount.com`，确认其持有 `roles/artifactregistry.serviceAgent`。清理了一条 `state: UNKNOWN` 的未上线 Function 半成品后重新部署，旧生产 Function 未触碰。
- 原因：Firebase Functions v2 需要 Artifact Registry 的 `gcf-artifacts` Docker 仓库；新 Blaze 项目的自动首次 provisioning 连续超时，需要排除 Secret、IAM、region、残留 Function 与 API 初始化问题。
- 影响：新项目的数据库、规则、认证、Secret 与两条迁移记录已就绪，但 `penneyMiniApi` 仍未上线，前端迁移分支因此没有合并到生产；`eden-tan.com` 继续使用旧 Poker Function，不会中断游戏。临时 `gcf-bootstrap` 创建也在产生资源前超时，因此没有留下额外仓库。
- 验证：Firebase Console 显示 Blaze / Pay as you go；Secret 规范化长度 old=64 / new=64 且哈希一致；排行榜 QA 输出 `anonymousAuth=true`、`scoreWrite=true`、`worseScoreRejected=true`、`publicRead=true`。Google Cloud 在 Singapore 与 us-central1 的 repository operations 均返回 `code: 4` / `provisioning for project \"eden-tan\" is taking too long to complete`，确认是项目级 Artifact Registry 后台阻塞，不是应用代码或区域配置。
- 后续：等 Google Cloud 完成新项目 provisioning 后重跑 `npm run functions:deploy`；新端点 GET 与 CORS 验证成功后再合并网站 PR，并精确删除旧 Poker 项目的 Eden Function、Secret 与数据库节点。

## 2026-08-19 16:06:20 +0800 — Mini Slot 过渡端点与可发布迁移边界

- 类型：Firebase / 发布策略 / 兼容迁移
- 改动：在用户明确同意后，把首页 Mini Slot 客户端的默认运行端点暂时保留为现有 Poker 项目中的 `penneyMiniApi`，避免合并后请求尚不存在的 `eden-tan` Function 而产生 404；`eden-tan` 仍是 `.firebaserc`、RTDB、Auth、规则、完整版排行榜、Functions 源码和所有部署命令的唯一目标。
- 改动：把这个过渡例外及移除条件写入 `docs/penney-mini-arena.md`、`state/current.md` 与 `AGENTS.md`；`verify:firebase` 改为只允许 `services/penneyMini.ts` 出现这一条精确旧端点，并继续拒绝其他活跃配置回指 Poker 项目。
- 原因：Blaze、API、账单、服务代理和 IAM 均正常，但 Google Artifact Registry 官方 API 与 Firebase Gen 1 / Gen 2 部署都持续以 `deadline_exceeded` 拒绝创建首个 `gcf-artifacts` 仓库；直接切换会破坏线上游戏，继续等待则无法完成用户要求的本次合并发布。
- 影响：本次可以安全合并并发布独立数据库与排行榜配置，线上 Mini Slot 继续可玩；每日 IP credits 与 Mini 排行榜在过渡期仍由旧 Function / 旧节点提供，因此后端隔离尚未最终完成。失败产生的 UNKNOWN Function 与 Gen 1 探针已精确删除，新项目 Functions 列表恢复为空。
- 验证：Cloud Billing API 返回 `billingEnabled: true`，Artifact Registry API 为 Enabled，四个相关服务代理角色齐全；直接创建仓库、Firebase Gen 1 探针和 Gen 2 正式函数都稳定返回相同项目级错误。发布前继续运行完整 `npm run check` 与 GitHub PR required check。
- 后续：Google provisioning 恢复后部署 `eden-tan/penneyMiniApi`，验证 GET、CORS、100 credits 与迁移榜单，再以独立小提交切换客户端并精确删除旧 Poker Function、Secret 与 `penneyMiniPlayers`。

## 2026-08-19 16:32:00 +0800 — 全 route 静态正文、结构化数据与双语 SEO

- 类型：SEO / Prerender / Structured Data / i18n / Performance
- 改动：新增 `seo-prerender.ts`，production build 现在为 37 条 registry route 同时生成英文与 `/zh/` 中文 HTML；每份服务器响应直接包含一个真实 H1、摘要、Wiki/Notes 完整结构化正文、可抓取主题集群内链、可见 breadcrumbs，以及按页面类型输出的 ProfilePage、BlogPosting、TechArticle、WebPage 或 ImageGallery JSON-LD。
- 改动：canonical、Open Graph URL、HTML lang、双向 hreflang 与 x-default 全部按稳定语言 URL 输出；sitemap 扩展为双语 URL，并加入 reciprocal alternate 和 `2026-08-19` lastmod。语言切换会导航到对应 canonical，中文页面的内部 registry 链接持续留在 `/zh/`。
- 改动：清理过长英文 title/description；把只有 localStorage 数据的 `/topics` 设为 noindex 并移出 sitemap；route harness 新增 title/description 长度约束，built-site smoke 新增双语 artifact、正文厚度、单 H1、普通内链、Breadcrumb JSON-LD、canonical、hreflang 与 sitemap lastmod 回归检查。
- 改动：`PenneysGamePage` 与首页 Mini Coin Slot 改为动态 import，独立生成 route chunk，降低不相关页面的初始 JavaScript；真实 Core Web Vitals trace 因当前工具环境缺少 Chrome performance trace 接口而未宣称完成。
- 原因：旧构建虽然让深链返回 200，但服务器 HTML 仍没有正文、H1、内链或 route schema；Google 需要执行整套客户端 JavaScript后才能理解页面，中文也没有稳定可索引 URL。
- 影响：搜索引擎与无 JavaScript 访客现在能直接读取页面主题、文章正文与关系图；英文和中文可独立抓取且不会互相 canonical；薄的本地问答板不再消耗索引预算。React 仍保留原互动体验，加载时替换语义等价的静态 fallback。
- 验证：TypeScript、16 个 unit tests、37 条 route contracts 与第一次 production build 已通过；build 产出独立 `PenneysGamePage` chunk，built-site smoke 已验证 74 份双语 route HTML。最终完整 `npm run check`、受保护 publish、线上 crawl 与 Search Console URL inspection 由本任务继续执行。
- 后续：部署后重新提交 sitemap，并对主页、`/project/`、`/wiki/`、一篇 Notes 与对应中文 URL 请求索引；Core Web Vitals 需在具有 Chrome DevTools trace 的环境中补测，不用 Lighthouse 猜测值代替。

## 2026-08-19 21:10:43 +0800 — Artifact Registry 恢复与独立 Function 上线

- 类型：Firebase / Cloud Functions / 基础设施恢复
- 改动：重试 `npm run functions:deploy` 后，Google Artifact Registry 成功创建 `eden-tan/asia-southeast1/gcf-artifacts`，Node.js 22 Gen 2 `penneyMiniApi` 已在独立 `eden-tan` 项目进入 ACTIVE；同时设置 1 天 Artifact Registry 镜像清理策略，避免旧构建镜像持续累积存储费用。
- 原因：先前项目级 provisioning 持续返回 `deadline_exceeded`，导致新 Function 无法建立；用户要求再次重试并确认是否恢复。
- 影响：独立 Function 现已具备正式切换条件，但生产 bundle 尚未改 URL，首页 Mini Slot 仍使用已批准的旧 Poker Function 兼容桥，因此本次不会中断线上游戏，也尚未删除旧资源。
- 验证：新端点 GET 返回 200、`Access-Control-Allow-Origin: https://eden-tan.com`、`dailyLimit: 100`、可读取排行榜；POST preflight 返回 204 且 CORS 正确。Firebase Functions 列表显示 `penneyMiniApi` 为 `state: ACTIVE`，`gcf-artifacts` 仓库存在，清理策略成功应用。
- 后续：从最新 `main` 开独立受保护任务，将 `services/penneyMini.ts` 默认 URL 切到 `eden-tan`，运行完整 harness、发布并读回生产；确认线上新 Function 正常后，再精确删除旧 Poker Function、Secret 与 `penneyMiniPlayers`。

## 2026-08-19 21:16:39 +0800 — Mini Slot 客户端切换到独立 Firebase

- 类型：Firebase / Mini Slot / 生产切换
- 改动：把 `services/penneyMini.ts` 的默认端点从旧 Poker 项目切换到 `https://asia-southeast1-eden-tan.cloudfunctions.net/penneyMiniApi`；移除临时兼容例外，并让 `verify:firebase` 对 Mini API 客户端同样强制执行 `eden-tan` 项目边界。
- 原因：`eden-tan` 的 Gen 2 Function 已进入 ACTIVE，GET、CORS、100-credit 合约和迁移排行榜验证完成，不再需要跨项目兼容桥。
- 影响：合并部署后，首页 Mini Slot 的 credits、server round、win rate 与排行榜写入都会由 Eden 独立项目处理。切换前再次比较新旧 `penneyMiniPlayers`：双方均为 2 位玩家、21 局、8 胜，按累计局数和更新时间计算没有需要增量覆盖的记录。
- 验证：本分支继续运行 `npm run check` 与受保护 publish；合并后必须读回生产 bundle，确认只含新端点，并验证新 Function GET / preflight 后，才可删除旧 Poker 项目的专属 Function、Secret 和数据库节点。
- 后续：完成线上读回和精确旧资源清理，再追加一条不可恢复资源删除记录。

## 2026-08-19 21:22:24 +0800 — Mini Slot 旧 Poker Firebase 资源退役

- 类型：Firebase / 生产验证 / 不可恢复清理
- 改动：PR #16 合并部署后，递归读取生产首页及 7 个 JavaScript chunks，确认只包含 `eden-tan` Mini API 端点且没有 `poker-power-card-3abea` 引用；再次验证新 Function GET 为 200、CORS origin 正确、`dailyLimit: 100`，POST preflight 为 204。
- 改动：生产切换门槛通过后，从旧 Poker 项目精确删除 `asia-southeast1/penneyMiniApi`、`PENNEY_IP_SALT` Secret、`penneyMiniPlayers` 和 `penneyLeaderboard` 两个 RTDB 节点。这些旧资源不可恢复；Mini Slot 的 2 位玩家、21 局、8 胜累计记录已在删除前确认保存在 `eden-tan`。
- 原因：完成 Eden 主站与 Poker 产品的 Firebase 数据、Secret 和运行时隔离，避免旧兼容资源继续产生混用或维护风险。
- 影响：Mini Slot credits、server round、win rate、Mini 排行榜和完整版排行榜现在都只使用 Eden 独立项目；旧 Function URL 已停止服务。
- 验证：旧项目 Functions 列表匹配数为 0，两个旧数据库节点均读回 `null`，旧 Secret 查询失败，旧端点返回 404；新端点在清理后仍返回 200。
- 后续：无；后续部署继续由 `npm run verify:firebase` 阻止活跃配置回指 sibling Firebase 项目。

## 2026-08-19 23:19:31 +0800 — Conway 页新增二进制与易经的结构桥梁

- 类型：Conway / 内容架构 / 数学解释 / 易经 / 双语 / SEO
- 改动：在 `/conways-game-of-life` 互动棋盘之后新增三段式 editorial 内容：先用 0 / 1 细胞与阴 / 阳爻说明共同的二元字母表，再用 `2³ = 8`、`2⁶ = 64`、`2⁸ = 256` 区分局部邻域、六十四卦状态空间与 Elementary Rules 规则空间，并展示 000—111 与八卦的形式配对。
- 改动：新增莱布尼茨历史时间线与 1703 年原文、伏羲卦图通信译文链接；明确二进制体系先于这次跨文化辨认，并区分邵雍 / 伏羲排列的特定读法与常用文王卦序，避免把形式对应误写成《易经》预言计算机或两者属于同一种自动机。
- 改动：同步中英文页面、route description、prerender 静态正文与 README route 说明；新布局沿用 Eden 的克制型 Apple Editorial 节奏，以大标题、细分隔线、数字信息岛与充足左右留白组织长内容，没有新增背景渐层或 glow 动效。
- 原因：用户希望 Conway 页面不仅可以玩，也能表达二进制、阴阳、八卦、六十四卦、256 条规则与莱布尼茨之间的结构关系，同时保持个人知识品牌的判断与阅读节奏。
- 影响：访客可从二维生命棋盘继续理解更广义的二元组合空间，并通过页面 CTA 进入现有 `/cellular-automata-lab` 实际探索 256 条一维规则；无 JavaScript 的中英文 route HTML 也会输出等价的三段核心说明。
- 验证：`git diff --check` 与 `npm run typecheck` 已通过；继续运行 `npm run log:index` 与完整 `npm run check`。
- 后续：无。

## 2026-08-19 23:22:34 +0800 — 首页 Conway Banner 替换为八卦金字塔影片

- 类型：首页 / Conway / Banner / 视频资源
- 改动：把首页滚动拼贴中指向 `/conways-game-of-life` 的原 CSS art Banner 替换为用户提供的 10 秒 16:9 八卦金字塔影片；输出无音轨、fast-start 的网页 MP4，并从代表画面生成 1280×720 poster。
- 改动：为新画面补充中英文替代文字；沿用首页既有的可见区域自动播放、离屏暂停、静音循环逻辑，移动端与 `prefers-reduced-motion` 模式继续显示静态 poster。
- 原因：让首页 Conway 入口直接使用新的「神秘交汇：八卦金字塔」视觉，并与页面新增的二进制 / 易经内容桥梁保持一致。
- 影响：首页 Conway 卡片的链接、CTA 与 16:9 布局不变；原 CSS art 仍保留在 `/project-css` 资产目录中，不影响其他页面。
- 验证：素材已确认是 H.264、1280×720、24fps；poster 尺寸与 Banner 比例一致。继续运行 `npm run log:index` 与完整 `npm run check`。
- 后续：无。
## 2026-08-19 23:38:56 +0800 — Home App icon 与 route PWA 范围分离

- 类型：Home / App icon / PWA / Manifest
- 改动：首页 Site PWA 改用独立且带新文件名的 `eden-home-app-icon.svg`，以粗体白色 Eden「E」作为 Home 标识；favicon 继续使用同一套 Eden 字母视觉，不再与 Conway 的细胞图标混淆。
- 改动：把 Conway 与 Film Gallery manifest 的 `scope` 和 `start_url` 收回各自 route，防止安装 route-specific PWA 后把网站首页也纳入其 App 范围；Service Worker 预缓存与动态 manifest 注入同步改用新的 Home icon 路径。
- 改动：新增单元测试，固定首页 manifest 图标、Home scope 与两个 route-specific scope，并把新 Home icon 纳入 built-site smoke 必需资产。
- 原因：截图显示 Home 被关联到 Conway 图标；根因是 Conway manifest 原先以 `./` 覆盖整站，而不是 Home 本身缺少 Eden icon。
- 影响：首页安装图标与 apple-touch icon 明确显示 Eden「E」；Conway 和 Film Gallery 仍保留自己的安装图标，但只在各自 route 范围内生效。
- 验证：继续运行 `npm run log:index` 与完整 `npm run check`。
- 后续：已安装过旧 Conway PWA 的设备可能需要删除旧安装后重新安装，才能立即刷新操作系统缓存的 App 关联与 icon。

## 2026-08-20 19:11:22 +0800 — 新增 /dr-racing 产品页与公开 Demo 嵌入

- 类型：Projects / 产品页 / CSS art / SEO / 双语
- 改动：新增 `/dr-racing` 产品页（`DrRacingFullPage`），沿用 `/etreporthub` 的 `ProductStorePage` 结构：kicker、tagline、内嵌 Demo stage、How a deal moves 四步、日常改变、FAQ 与 specs，全部中英双语；Demo iframe 指向 `https://edent95.github.io/dr-racing-dashboard-demo/demo/`（公开 Demo，固定匿名示例数据，免登录，浏览器内运行，刷新即重置）。
- 改动：新增 `ProjectsDrRacingCssIcon` 转速表 CSS app icon（摆动指针、红区、速度线，含 dark mode 与 prefers-reduced-motion 静态姿态），注册进 `css-art.registry.ts`；icon 样式追加在 `styles/css-art/projects-icons.css`。
- 改动：`/project` app shelf 新增 Dr Racing 卡片（caption：Loan pipeline / 贷款流水线）；`productSiblings` 加入 Dr Racing，使各产品页的「You might also like」互链包含它；`seo-routes.ts` 注册 route（title 49 字符、desc 156 字符），README route 列表同步。
- 原因：用户希望 Dr Racing 像 ETReportHub 一样拥有 eden-tan.com 产品页，并附带可直接试玩的公开 Demo；Demo 站点已按 daily-report-dashboard 相同模式生成（独立 GitHub Pages 仓库 dr-racing-dashboard-demo）。
- 影响：路由数从 37 增至 38；其他产品页的互链行会多出 Dr Racing 卡片；在 Demo 仓库推送并开启 GitHub Pages 之前，产品页内的 iframe 会暂时显示空白。
- 验证：`npm run typecheck`（除 functions 目录被排除的 penney-mini 测试引用外无错误）、`node scripts/harness/check-routes.mjs`（38 route contracts pass）与 `node scripts/harness/check-css.mjs`（12 CSS art families pass）已在隔离构建环境通过；`vite build` 成功并对 `/dr-racing` 与 `/project` 截图确认渲染。继续运行 `npm run log:index` 与完整 `npm run check`。
- 后续：待 dr-racing-dashboard-demo 仓库上线 GitHub Pages 后确认 iframe 正常载入。

## 2026-08-20 20:03:00 +0800 — 首页新增 Dr Racing 影片 Banner

- 类型：首页 / Dr Racing / Banner / 视频资源
- 改动：把用户提供的 `dr-racing.mp4` 接入首页滚动项目胶片墙，位置紧接 ETReportHub；Banner 点击进入 `/dr-racing`，并补充中英文无障碍描述与 CTA。
- 改动：影片输出为 1280×720 H.264、无音轨、fast-start 的网页 MP4，并从代表画面生成同尺寸 poster；继续复用首页既有的可见区域自动播放、离屏暂停、静音循环与 reduced-motion 静态降级逻辑。
- 原因：让首页直接展示 Dr Racing 的摩托车视觉，并把新上线的产品页纳入主页项目发现路径。
- 影响：首页胶片墙新增一个 16:9 Dr Racing 项目入口；其他卡片的顺序、链接和播放逻辑不变。
- 验证：源影片已确认是 H.264、1280×720、24fps、10 秒；继续运行 `npm run log:index` 与完整 `npm run check`。
- 后续：无。

## 2026-08-20 20:04:14 +0800 — 首页 Hero 移除 Personal Lab CTA

- 类型：首页 / Hero / CTA
- 改动：移除首页 Hero 的 `Enter my personal lab / 进入我的个人实验室` 文字按钮，保留主要的 `Explore my work / 探索我的作品` CTA。
- 原因：用户希望首页首屏只保留作品入口，减少重复行动与视觉干扰。
- 影响：首页 Hero 现在只有一个主要按钮；Jiju 产品页、其他 Lab 入口和导航均不受影响。
- 验证：继续运行 `npm run log:index` 与完整 `npm run check`。
- 后续：无。

## 2026-08-26 01:11:23 +0800 — Life OS 应用域名迁移

- 类型：域名迁移 / Life OS / 外链
- 改动：把 `/life-os` 产品页的主 CTA 与内嵌应用地址从 `https://edent95.github.io/8g-master/` 更新为 `https://edent95.github.io/LifeOs/`，并同步 iframe 显示域名。
- 原因：Life OS 应用已经迁移到新的 GitHub Pages 仓库路径。
- 影响：访客点击「Open Life OS / 打开 Life OS」或使用页面内互动预览时，都会进入新的 `LifeOs` 地址；产品页其他内容与路由不变。
- 验证：运行旧域名全局残留检查、`npm run log:index` 与完整 `npm run check`。
- 后续：无。
